//Random Helper functions

#ifndef COOPERTOOLS_HPP
#define COOPERTOOLS_HPP

#include <opencv/highgui.h>

//int abs(int i); //absolute value
float abs(float i); //absolute value
int distOverlap(int p1, int p2, int p3, int p4); //returns length of overlap of 2 line segments in 1D space
void drawX(IplImage *img, CvPoint p); //draws an x on an image

//Degree tools:
float normDeg(float deg); //puts any degree value onto the -180 to 180 circle
int inDegSeg(float valueDeg, float startingDeg, float endingDeg); //checks if valueDeg is inbetween startingDeg and endingDeg
float degDiff(float deg1, float deg2); //returns degree offset between the two degrees (-180 to 180)

#endif
